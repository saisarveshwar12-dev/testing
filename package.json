{
  "name": "voice-test",
  "version": "1.0.0",
  "description": "LiveKit Voice Test App - Vercel deployment",
  "private": true,
  "dependencies": {
    "livekit-server-sdk": "^2.6.0"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev:backend": "node backend/server.js",
    "dev:frontend": "cd frontend && npm run dev"
  }
}
